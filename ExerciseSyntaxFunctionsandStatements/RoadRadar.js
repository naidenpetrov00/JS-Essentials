function solve(speed, area) {
    let limits = {
        motorway: 130,
        interstate: 90,
        city: 50,
        residental: 20
    }

    let areas = Object.keys(limits);

    switch (area) {
        case Object.keys(limits)[0]:{
        }
    }
}

solve([40, 'city']);